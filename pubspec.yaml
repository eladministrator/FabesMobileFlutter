name: fabes
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.17.1 <3.0.0"
dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.11.0
  flutter_secure_storage: ^6.0.0
  http: ^0.13.5
  local_auth: ^2.1.2
  provider: ^6.0.3
  data_table_2: ^2.3.7
  intl: ^0.17.0
  firebase_core: ^2.3.0
  firebase_messaging: ^14.1.2
dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/Logo_fabes.png"
  adaptive_icon_background: "assets/Logo_fabes.png"
flutter:
  uses-material-design: true
  assets:
    - assets/fabes_master.png